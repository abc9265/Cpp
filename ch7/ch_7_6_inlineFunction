/*
	Inline Function
*/

#include <iostream>

using namespace std;

// 작은함수가 아주 많이 반복되는경우 
// 실제 계산하는 부분 보다 값을 찾는 일련의 과정들이 시간이 더 많이 걸릴때도 있음.
// 이걸 줄이기 위해 인라인 함수를 쓰는경우가 있음
// 헤더파일에 함수를 선언할때 많이 씀.
// 인라인으로 바꾸게되면 컴파일러내에서 함수가 아닌거처럼 정의가된다.
// 인라인 키워드는 강제로 함수를 인라인해라라고 말하는게아닌
// 권장, 권유하는 뉘앙스임 
// 요즘 컴파일러는 좋아져서 인라인 넣으면 빨라지겠다 싶으면 알아서 넣음
// 소프트웨어의 구조의 변경을 통해 최적화하는것이 좋음
// 인라인 함수가 정말 많으면 컴파일된 프로그램이 많이 커짐.
// 코드가 너무 커져서 인라인의 효과가 없을 수도 있음.
inline int min(int x, int y)
{
	return x > y ? y : x;
}


int main()
{
	cout << min(5, 6) << endl;
	cout << min(3, 2) << endl;	
	
	cout << (5 > 6 ? 6 : 5) << endl;
	cout << (3 > 2 ? 2 : 3) << endl;
	
	return 0;
}