/*
	Control Flow
	제어 흐름
	순서도 - Flow Chart
	쭉쭉쭉쭉 가는거 sequence
	중단: Halt
	점프: Jump // goto, break, continue
	조건 분기 Conditional branches // if, switch
	반복 : Loops // while, do while, for
	예외처리 Excaeptions // try, catch
*/

#include <iostream>
#include <cstdlib>
int main()
{
	using namespace std;
	
	cout << "I love you" << endl;
	
	// 도망가고싶다
	// cstdlib라이브러리 인클루드 필요
	// 코드의 어디에 있든지 정수 하나만 넣어주면 종료 시킬 수 있음. 
	// return에 비해 리턴타입을 안맞춰도 그냥 바로 끌 수 있다는 장점이 있음 
	// 긴급탈출, 무조건 여기서 끝내
	// 프로그래밍 하는 과정에 분석을 위해서 그때그때 사용하는 경우가 더 많을 수 있다.
	exit(0); 
	// return 0;
	
	// .... 
	cout << "I love you too" << endl;
	
	return 0;
}