/*
	산술 연산자 arithmetic operators
	단항연산자: +, -
	이항연산자: *, / 
	나머지연산자 : % - 나머지 출력
	
*/

#include <iostream>

int main()
{
	using namespace std;
	
	int x = 7;
	int y = 4; // - x 이런식으로 띄워서 하는 경우가 있는데 사람에게 헷갈릴 수 있음.
	int z = x % y;
	
	
	// 암묵적 casting
	// '컴파일러가 그냥 일단 하더라'라고 일단은 이해하기
	// 주의사항: 음수를 나누는 경우가 생기는데 
	// 옛날 컴퓨터면 -2.5면 -2로할지 -3으로할지 오락가락했는데
	// c++ 11부터는 그냥 소수점이하는 버려버림
	// 나머지 연산도 나머지가 음순지 양순지 애매할 수 있는데 
	// 왼쪽에 있는 숫자가 음수면 나머지 연산의 숫자도 음수
	// 양수면 나머지도 양수인걸로 c++11에서 규정함.
	cout << x / y << " " << z << endl;
	cout << float(x) / y << endl; // 하나만 실수형이면 값도 실수형으로 나옴
	cout << x / float(y) << endl;
	cout << float(x) / float(y) << endl;
	cout << -5 / 2 << endl; // 음수를 나눔.
	
	// 등호는 대입연산자인데
	// x변수가 가리키고있는 메모리에있는 정보를
	// k변수가 가리키고있는 메모리에 가져다가 넣어라라는 뜻임
	int k = x;  
	k += y; // z = z + y; 두개 다 정확하게 똑같이 작동함
	cout << k << endl;
	
	
	return 0;
}